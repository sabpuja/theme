{{ 'sabpuja-pages.css' | asset_url | stylesheet_tag }}
<section class="sp-page-shell">
  <div class="sp-page-hero">
    <p class="sp-page-eyebrow">Sabpuja</p>
    <h1>{{ page.title }}</h1>
  </div>
  <div class="sp-prose rte">{{ page.content }}</div>
</section>
